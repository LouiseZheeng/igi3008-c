// primes.c

#include <stdio.h>
#include <math.h>

int isprime(int p)
{
    for (int i=2; i<=sqrt(p); i++)
    {
        if (p<=1)
        {
            return 0;
        }
        else if (p%i==0)
        {
            return 0;
        }
    }
    return 1;
}

int main()
{
    L=[]
    if isprime(p)
    {
        L.append(p)
        c=len(p)
        return c
    }
    return 0;
}